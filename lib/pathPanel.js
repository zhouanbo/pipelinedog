var React = require('react');

var PathPanel = React.createClass({
  displayName: 'PathPanel',

  render: function () {
    return React.createElement(
      'p',
      null,
      'aaa'
    );
  }
});

module.exports = PathPanel;